# ClearML Server + client configuration (local-only).
#
# 1) Start server:
#    cd iris-mlops/clearml
#    cp .env.example .env
#    docker compose --env-file .env -f docker-compose.yml up -d
#
# 2) Export vars for the client (same values):
#    export CLEARML_API_HOST="http://localhost:8008"
#    export CLEARML_WEB_HOST="http://localhost:8080"
#    export CLEARML_FILES_HOST="http://localhost:8081"
#    export CLEARML_API_ACCESS_KEY="..."
#    export CLEARML_API_SECRET_KEY="..."
#
# Hosts (docker-compose uses CLEARML_FILES_HOST for async_delete config)
CLEARML_API_HOST=http://localhost:8008
CLEARML_WEB_HOST=http://localhost:8080
CLEARML_FILES_HOST=http://localhost:8081

# Local API credentials (NOT a production secret; used only for your local server)
CLEARML_API_ACCESS_KEY=hw5_local_access
CLEARML_API_SECRET_KEY=hw5_local_secret

